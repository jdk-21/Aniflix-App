import 'package:flutter/material.dart';

class SubBox extends StatelessWidget {
  @override
  Widget build(BuildContext ctx) {
    return Center(child: Text('Abos', style: TextStyle(fontSize: 24.0)));
  }
}
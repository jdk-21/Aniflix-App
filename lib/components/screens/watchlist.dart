import 'package:flutter/material.dart';

class Watchlist extends StatelessWidget {
  @override
  Widget build(BuildContext ctx) {
    return Center(child: Text("Watchlist", style: TextStyle(fontSize: 24.0)));
  }
}
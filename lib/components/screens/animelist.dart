import 'package:flutter/material.dart';

class AnimeList extends StatelessWidget {
  @override
  Widget build(BuildContext ctx) {
    return Center(key: Key("all_screen"),
        child: Text('Anime Liste', style: TextStyle(fontSize: 24.0)));
  }
}
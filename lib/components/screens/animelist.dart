import 'package:flutter/material.dart';

class AnimeList extends StatelessWidget {
  @override
  Widget build(BuildContext ctx) {
    return Center(child: Text('Anime Liste', style: TextStyle(fontSize: 24.0)));
  }
}
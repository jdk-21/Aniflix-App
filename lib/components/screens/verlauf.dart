import 'package:flutter/material.dart';

class Verlauf extends StatelessWidget {
  @override
  Widget build(BuildContext ctx) {
    return Center(child: Text("Verlauf", style: TextStyle(fontSize: 24.0)));
  }
}